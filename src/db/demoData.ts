import { Mission, Equipment, MissionEquipment, Vehicle, Staff, Incident } from '../types';

export const demoMissions: Mission[] = [];
export const demoEquipment: Equipment[] = [];
export const demoMissionEquipment: MissionEquipment[] = [];
export const demoVehicles: Vehicle[] = [];
export const demoStaff: Staff[] = [];
export const demoIncidents: Incident[] = [];

